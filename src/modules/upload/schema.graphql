scalar Upload

type Mutation {
  upload(file: Upload!): File!
  uploadMany(files: [Upload!]!): [File!]!
}

type File {
  filename: String!
  mimetype: String!
  encoding: String!
}

type Query {
  uploads: [File]
}
