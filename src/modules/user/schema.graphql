type Query {
  user(username: String!): User
}

type User {
  id: String!
  username: String!
  password: String!
  email: String!
  role: String!
  token: String!
}

type Mutation {
  createUser(email: String!, username: String!, password: String!): User
}

type Subscription {
  users: User
}
