type Query {
  user(username: String!): User
  users: [User!]!
  hello: String @upper
}

type User {
  id: String!
  username: String!
  password: String!
  email: String!
  role: String!
  token: String!
}

type Mutation {
  createUser(email: String!, username: String!, password: String!): User
}

type Subscription {
  users: User
}
